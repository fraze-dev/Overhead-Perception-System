# Overhead Perception System - Dependencies
# Python 3.13+
#
# Note: pyrealsense2 must be installed separately via the Intel RealSense SDK:
#   https://github.com/IntelRealSense/librealsense/releases/tag/v2.57.5
#   Or via pip: pip install pyrealsense2
#   (pip install may not include all SDK features on all platforms)

numpy==2.2.4
opencv-python==4.12.0.88